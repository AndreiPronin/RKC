@using BE.JobManager;
@{
    ViewBag.Title = "ParilaViewIntegration";
}

<br />
<div style="margin-top:30px;" class="row">
    <div class="col-md-4">
        <button id="ExaminationPuIsLic" class="btn btn-primary">Проверка ПУ наличие в лик</button>
    </div>
    <div class="col-md-4">
        <button id="ExaminationPersIsLic" class="btn btn-primary">Проверка Перс наличие в лик</button>
    </div>
    <div class="col-md-4">
        <button id="ExaminationDublicateNumberPu" class="btn btn-primary">Проверка дубликатов номеров ПУ</button>
    </div>
</div>
<div id="ExaminationResult"></div>
<br />
<script>
    $(document).ready(function (e) {
        $("#ExaminationPuIsLic").click(function () {
            $("#ExaminationPuIsLic").addClass("disabled");
            window.location.href = "/Counter/ExaminationPuIsLic";
            $("#ExaminationPuIsLic").removeClass("disabled");
        })
        $("#ExaminationPersIsLic").click(function () {
            $("#ExaminationPersIsLic").addClass("disabled");
            window.location.href = "/PersonalData/ExaminationPersIsLic";
            $("#ExaminationPersIsLic").removeClass("disabled");
        })
        $("#ExaminationDublicateNumberPu").click(function (e) {
            $.ajax({
                url:  `/api/ApiJob/${@((int)EnumJob.CheckDublicatePuNumberPu)}`,
                success: function (data) {
                    alert("Отчет отправлен на почту");
                    window.location.reload();
                },
                error: function () {
                }
            });
        })
    });
</script>

